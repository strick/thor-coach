# Thor Pi Voice Client Configuration

# Thor Agent URL (the agent runner service)
THOR_AGENT_URL=http://localhost:3002

# Speech-to-Text Configuration
STT_MODE=api                # "local" or "api"
WHISPER_MODEL=base          # For local: tiny, base, small, medium, large
OPENAI_API_KEY=             # Required for API mode

# Text-to-Speech Configuration
TTS_ENGINE=pyttsx3          # "pyttsx3" or "espeak"

# Audio Configuration
SAMPLE_RATE=16000           # Sample rate in Hz
RECORD_DURATION=5.0         # Recording duration in seconds
