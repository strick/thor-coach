# Thor Stack - Docker Environment Configuration
# Copy this file to .env.docker and configure for your setup

# ============================================
# LLM Configuration
# ============================================

# Use local Ollama (set to "true") or OpenAI (set to "false")
USE_OLLAMA=true

# Ollama Configuration (if USE_OLLAMA=true)
# Note: host.docker.internal allows Docker containers to reach host machine
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.1:8b

# OpenAI Configuration (if USE_OLLAMA=false)
# Uncomment and set your API key
# OPENAI_API_KEY=sk-your-api-key-here

# ============================================
# Strava Integration
# ============================================
# Strava API Configuration for syncing running activities
# Get these from https://www.strava.com/settings/api
# STRAVA_CLIENT_ID=your-client-id
# STRAVA_CLIENT_SECRET=your-client-secret
# STRAVA_REFRESH_TOKEN=your-refresh-token

# ============================================
# Service Ports (usually no need to change)
# ============================================
# API_PORT=3000
# WEB_PORT=3001
# AGENT_PORT=3002

# ============================================
# Notes
# ============================================
# - Database is automatically persisted in Docker volume "thor-data"
# - To use Ollama from Docker, ensure Ollama is running on host machine
# - For OpenAI, set USE_OLLAMA=false and provide OPENAI_API_KEY
# - All services communicate via internal Docker network "thor-network"
